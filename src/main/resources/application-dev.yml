spring:
  datasource:
    url: jdbc:mysql://localhost:3306/product_db
    username: admin
    password: admin
  data:
    redis:
      host: localhost
      port: 6739
      username: admin
      password: admin
eureka:
  client:
    enabled: false
  instance:
    hostname: localhost